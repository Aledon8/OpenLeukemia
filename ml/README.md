# ML workspace

This directory will contain:

- `training/` for training pipelines;
- `inference/` for runtime prediction code;
- `evaluation/` for validation and model quality reports;
- `notebooks/` for exploratory work that has not yet become production code.

Structured models should remain separate from LLM explanation workflows.
